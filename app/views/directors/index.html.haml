-index = 1
-@directors.each_with_index do |director, index|
  #{index + 1}.
  =link_to director.name, director_path(director)
  %br
  %br
=link_to "Register", new_director_path
-# =link_to "Go to EDIT", edit_director_path(@director)
-# =link_to "Go to DELETE", director_path(@director), method: :delete, data: {confirm: "Want to delete?"}
